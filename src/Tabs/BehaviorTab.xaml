<UserControl x:Class="FluxTranslator.Tabs.BehaviorTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="28,16,28,24">

            <TextBlock Style="{StaticResource H1}" Text="Behavior"/>

            <TextBlock Style="{StaticResource H2}" Text="Behavior"/>
            <Border Style="{StaticResource SettingCard}">
                <StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="180"/><ColumnDefinition Width="40"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Overlay Display Time" Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="13" VerticalAlignment="Center"/>
                        <Slider x:Name="SlDisplayTime" Grid.Column="1" Minimum="2" Maximum="60" TickFrequency="1"
                                IsSnapToTickEnabled="True" Style="{StaticResource ModernSlider}"
                                VerticalAlignment="Center" Margin="0,3,0,0"
                                ValueChanged="SlDisplayTime_ValueChanged"/>
                        <TextBlock x:Name="LblDisplayTime" Grid.Column="2" VerticalAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="6,0,0,0"/>
                    </Grid>

                    <Separator Margin="0,14,0,14"/>

                    <TextBlock Text="Speech Capture Mode"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="13"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Manual mode requires pressing the translate hotkey again to finish recording. Auto mode stops after silence."
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,10"/>

                    <StackPanel>
                        <RadioButton x:Name="RbManualMode"
                                     Content="Manual mode"
                                     Margin="0,0,0,8"
                                     Checked="RecordingMode_Checked"/>
                        <RadioButton x:Name="RbAutoMode"
                                     Content="Auto mode"
                                     Checked="RecordingMode_Checked"/>
                    </StackPanel>


                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
